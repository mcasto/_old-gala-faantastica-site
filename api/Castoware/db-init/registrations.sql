CREATE TABLE registrations (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    `name` text,
    `email` text,
    `whatsapp` text,
    `num_tickets` integer,
    `num_vip` integer,
    `table` integer,
    `table_name` text,
    `date` text
);